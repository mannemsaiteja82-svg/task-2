name: Portfolio Deployment on S3

on:
  push:
    branches:
    - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ AKIASFBUJKAQ7EKHFBK3 }}
        aws-secret-access-key: ${{ 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 }}
        aws-region: ap-south-1

    - name: Deploy static site to S3 bucket
      run: aws s3 sync . s3://info11k-port --delete
